<h1 class="large">
    <%= title %>
</h1>

<p>
    On <a href="<%= url %>"><%= url %></a>
</p>

<% if (process.env.NODE_ENV === 'development') { %>
    <pre>
        <code>
    <%= stack %>
        </code>
    </pre>
<% } %>

<% if (back_url) { %>
    <a href="<%= back_url %>">Go back</a>
<% } %>