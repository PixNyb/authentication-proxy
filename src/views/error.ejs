<h1>
    <%= title %>
</h1>

<p>
    <%= message %>
</p>

<% if (process.env.NODE_ENV === 'development') { %>
    <pre>
        <code>
    <%= stack %>
        </code>
    </pre>
<% } %>

<% if (back_url) { %>
    <a href="<%= back_url %>">Go back</a>
<% } %>